# Your system has configured 'manage_etc_hosts' as True.
# As a result, if you wish for changes to this file to persist
# then you will need to either
# a.) make changes to the master file in /etc/cloud/templates/hosts.tmpl
# b.) change or remove the value of 'manage_etc_hosts' in
#     /etc/cloud/cloud.cfg or cloud-config from user-data
#
127.0.0.1 localhost

# The following lines are desirable for IPv6 capable hosts
::1 ip6-localhost ip6-loopback
fe00::0 ip6-localnet
ff00::0 ip6-mcastprefix
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters
ff02::3 ip6-allhosts

{% for host in groups[teleport_hostgroup] %}
{% if teleport_override_system_hostname %}{% set hostname = hostvars[host]['inventory_hostname'] %}{% else %}{% set hostname = hostvars[host]['ansible_hostname'] %}{% endif %}
{{ hostvars[host]['teleport_host_ip'] }} {{ hostname }}{% if teleport_domain != "" %}.{{ teleport_domain }} {{ hostname }}{% endif %} {{ hostvars[host]['teleport_additional_aliases']|default("") }}
{% endfor %}
